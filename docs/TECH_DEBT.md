# Tech Debt

This document tracks deliberate shortcuts, gaps, and cleanup work.

## Current Debt

- The documentation structure is now present, but most technical sections are placeholders until the app grows.
- The repository has no `lib/` or `types/` directories yet even though the architecture reserves them.
- There is no `app/api/` implementation yet.
- There is no Supabase integration yet.
- There is no OpenAI integration yet.
- The repository has no configured `npm test` script yet.

## How To Use This File

- Add one entry per debt item.
- Include why the shortcut was taken.
- Include the impact.
- Include the intended fix.
- Remove entries when resolved.
